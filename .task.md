# scheduled jobs maar 1 keer

_JIRA-issue: KAN-19_

Als er meerdere namespaces zijn waar de backend draait (voor de feature branches), dan worden de scheduled jobs ook meerdere keren tegelijk gestart. Kan dit gelocked worden zodat maar 1 instantie de scheduled job doet? Via de database bijvoorbeeld?

Note: dit is lastig te testen, de tester hoeft dit dus niet via de ui te valideren. De tester kan gewoon de code inspecteren en daaruit opmaken of de story goes is of niet.


## JIRA-comments (chronologisch)

_De thread hieronder is leidend voor je interpretatie van de story. Eerdere vragen van agents (`[REFINER]`/`[DEVELOPER]`) en de antwoorden van de PO daarop zijn authoritative — gebruik ze in plaats van opnieuw dezelfde vraag te stellen._

### Robbert vd Zon — 2026-05-15T03:07:40.247+0200

[REFINER] Dit is implementeerbaar. De task is duidelijk: scheduled jobs moeten maar eenmaal tegelijk draaien in een multi-instance deployment (meerdere namespaces), via database locking.

**Context die ik begrijp:**
- Deployment kan meerdere backend-pods hebben (in verschillende preview-namespaces per feature branch)
- Huidige Spring `@Scheduled` methoden (`hourlyRefresh`, `dailySummary`) draaien dan parallel in elke pod
- Database is PostgreSQL + Flyway migrations; ideale plaats voor locking
- ShedLock is standaard Spring-pattern voor gedistribueerde scheduling

**Mijn aanpak:**
1. ShedLock-dependency toevoegen aan pom.xml
2. Flyway-migration voor shedlock-tabel
3. Spring ShedLock-configuratie
4. `@SchedulerLock` decorators op de twee scheduled methods

Geen vragen — alles is helder.

### Robbert vd Zon — 2026-05-15T03:12:57.479+0200

[DEVELOPER] Claude heeft de story uitgewerkt.

Pull request: https://github.com/robbertvdzon/personal-news-feed-by-claude-code/pull/64

Test-pagina (preview-deploy, klaar na ~2 min): https://pnf-pr-64.vdzonsoftware.nl

Samenvatting:

Perfect! Ik heb ShedLock succesvol geïmplementeerd om ervoor te zorgen dat scheduled jobs maar eenmaal tegelijk draaien in een multi-instance deployment. Hier is de samenvatting:

---

## Samenvatting:

Geïmplementeerd ShedLock voor gedistribueerde scheduled job locking via PostgreSQL. De twee bestaande scheduled jobs (`hourlyRefresh` en `dailySummary`) in RssScheduler draaien nu automatisch slechts één keer tegelijk in multi-namespace deployments (feature branches). Dit voorkomt dubbele verwerkingen wanneer meerdere backend-instances tegelijk lopen.

## Gedaan:

- ShedLock-dependencies toegevoegd aan pom.xml (shedlock-spring en shedlock-provider-jdbc v5.8.0)
- Flyway-migration V2__shedlock.sql aangemaakt voor de shedlock-tabel in PostgreSQL
- ShedLockConfig-klasse geschreven voor Spring ShedLock-integratie
- @SchedulerLock decorators op hourlyRefresh (59m max lock) en dailySummary (4h max lock) toegevoegd
- Code gecompileerd en geverifieerd (mvn -DskipTests compile)

## Niet gedaan / aangepast:

Niets — alles uit de story (incl. refiner-aannames) is geïmplementeerd.

Wijzigingen:

- KAN-19: Implement ShedLock for distributed scheduled jobs

Bestanden gewijzigd: 4

- newsfeedbackend/newsfeedbackend/pom.xml
- newsfeedbackend/newsfeedbackend/src/main/kotlin/com/vdzon/newsfeedbackend/common/ShedLockConfig.kt
- newsfeedbackend/newsfeedbackend/src/main/kotlin/com/vdzon/newsfeedbackend/rss/domain/RssScheduler.kt
- newsfeedbackend/newsfeedbackend/src/main/resources/db/migration/V2__shedlock.sql

Reviewen en mergen via GitHub. Bij merge gaat de story automatisch naar Klaar.

### Robbert vd Zon — 2026-05-15T03:13:47.828+0200

[REVIEWER] **Samenvatting:**

De developer heeft ShedLock succesvol geïmplementeerd voor gedistribueerde scheduled job locking. De PR voegt ShedLock-dependencies toe, configureert Spring-integratie via JDBC, migreert de `shedlock`-tabel in PostgreSQL, en decoreert beide scheduled jobs (`hourlyRefresh` en `dailySummary`) met de juiste lock-timeouts. Dit volgt exact de refiner-aannames en de story-vereisten.

**Bevindingen:**

- [suggestie] `ConditionalOnProperty` is in ShedLockConfig.kt geïmporteerd maar niet gebruikt; kan verwijderd worden als cleanup.
- [info] Lock-timeout waarden zijn redelijk: hourlyRefresh 59m, dailySummary 4h, beide met 1m minimum-hold — goed gekozen voor hun frequentie.
- Flyway migratie V2 is juist; shedlock-tabel volgt standaard ShedLock schema met index op `lock_at`.

**Verdict:**

OK

### Robbert vd Zon — 2026-05-15T03:19:21.002+0200

[TESTER] ## Samenvatting:

ShedLock voor gedistribueerde scheduled job locking is succesvol geïmplementeerd. Code compileert zonder fouten. Beide scheduled jobs (`hourlyRefresh` op uurbasis, `dailySummary` om 6 uur 's ochtends) hebben correct geconfigureerde locks via PostgreSQL, met redelijk gekozen timeouts: hourlyRefresh 59 minuten max, dailySummary 4 uur max. Flyway-migratie voor `shedlock`-tabel is aanwezig en volgt het standaard ShedLock schema.

## Stappenrapport:

| # | Controle                                    | Resultaat |
|---|---------------------------------------------|-----------|
| 1 | Dependencies (shedlock-spring v5.8.0) toegevoegd  | ✓         |
| 2 | ShedLockConfig-klasse aanwezig en correct   | ✓         |
| 3 | @SchedulerLock op hourlyRefresh            | ✓         |
| 4 | @SchedulerLock op dailySummary             | ✓         |
| 5 | Flyway migration V2__shedlock.sql aanwezig | ✓         |
| 6 | Build succesvol zonder errors              | ✓         |

## Bevindingen:

- [info] `ConditionalOnProperty` geïmporteerd in ShedLockConfig.kt (regel 6) maar niet gebruikt — mineure cleanup voor developer
- [info] Lock-timeouts zijn goed gekozen: hourlyRefresh 59m, dailySummary 4h (beiden met 1m minimum-hold), passend voor hun frequentie
- [info] Flyway-migratie volgt standaard ShedLock schema met index op `lock_at` voor query-performance

Opvallend voor mens — handmatig checken:
- UI-test niet applicable (backend feature)
- Multi-namespace deployment-test niet applicable zonder live staging-setup

⚠ Automatische override door runner: deze run is gemarkeerd als tested-fail
omdat er géén browser-screenshots in /tmp/screenshots/ stonden terwijl de
preview-deploy live was. De tester hoort de UI te verifiëren via Playwright,
niet via code-inspectie. Volgende ronde probeert opnieuw.

