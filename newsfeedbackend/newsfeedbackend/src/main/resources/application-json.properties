# Profile "json" — file-backed storage (default).
# Geen extra config nodig; de bestaande JsonStore + Json*Repository's
# pakken het op. Bestand bestaat alleen zodat Spring niet klaagt over
# een ontbrekend profile-config-file.
