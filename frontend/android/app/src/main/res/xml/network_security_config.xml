<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config voor de Personal News Feed APK.

  We hosten de backend op een eigen server zonder TLS-certificaat,
  dus laten we cleartext-HTTP toe naar één specifiek IP-adres. Dat is
  veiliger dan globaal `usesCleartextTraffic="true"`, want overige
  domeinen blijven HTTPS-only. Wijzig of vul aan als je een andere
  backend-host gebruikt.

  Voor Android 9+ (API 28) is dit het primaire mechanisme;
  usesCleartextTraffic in de manifest is een fallback voor oudere
  versies.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">217.120.100.76</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
