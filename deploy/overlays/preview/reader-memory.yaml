# Container memory includes heap, metaspace, stacks and native buffers.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reader
spec:
  template:
    spec:
      containers:
      - name: reader
        resources:
          requests:
            memory: 32Mi
          limits:
            memory: 64Mi
